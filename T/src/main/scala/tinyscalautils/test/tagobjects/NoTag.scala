package tinyscalautils.test.tagobjects

import org.scalatest.Tag

/** A tag that represents the absence of a tag. This is used so tags can be present conditionally.
  *
  * @since 1.1
  */
object NoTag extends Tag("tinyscalautils.test.tags.NoTag")
